[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npx tsx prisma/seed-prod.ts && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
