# 해야할 것

## 완료

### 보안
- [x] Professor 테이블 password 필드 bcrypt 해싱 적용
- [x] 업로드 API 인증 체크 + 강의 소유권 검증
- [x] Server Actions 소유권 검증 추가 (analyzeMaterial, analyzeCauses, compareRuleVsLlm, filterComments, getTokenStats)

### 인증
- [x] NextAuth.js v5 교수 로그인 구현
- [x] FeedbackToken 모델 추가 (DB 스키마)
- [x] 교수가 1회용 토큰 링크 생성하는 기능 (batch insert)
- [x] 토큰 검증 → 피드백 페이지 접근 제어
- [x] 피드백 제출 시 토큰 소멸 처리 (트랜잭션)

### 기능
- [x] 교수 대시보드 (3축 피드백 분석 + 응답수 경고)
- [x] 강의자료 분석 (PDF/PPT/TXT + 스캔 PDF OCR)
- [x] 공지/메일 톤 보정
- [x] 데모 데이터 세팅

### 버그 수정
- [x] 홈 페이지 토큰 없는 피드백 링크 → 랜딩 페이지로 변경
- [x] communication 검증 로직 수정 (NaN 체크)
- [x] html lang="en" → lang="ko"
- [x] seed에서 feedbackToken 테이블 정리 누락

### 리팩터링
- [x] OpenAI 호환 어댑터 중복 제거 (openai-compatible.ts 공통 팩토리)
- [x] 토큰 생성 루프 → createMany 배치 삽입
- [x] 강의 대시보드 페이지 들여쓰기 정리
- [x] ESLint 에러 수정 (unused import, any 타입)
- [x] cause-analysis: JSON parse 안전하게 try-catch 처리
- [x] rule-vs-llm: 규칙 분류기 "첫 매칭 우선" → "다수 매칭 우선" 개선
- [x] rule-vs-llm: LLM 응답 배열 유효성 검증 추가
- [x] 공유 상수 constants.ts 분리 (FEEDBACK_MIN_COUNT, COMM_AVG_THRESHOLD 등 임계값)
- [x] PDF 추출 로직 file-extraction.ts 분리 (analyze-material.ts에서 분리)
- [x] AI JSON 파싱 parse-ai-json.ts 분리 (마크다운 펜스 제거 + 첫 JSON 블록 추출)
- [x] 백그라운드 분류 큐 classify-queue.ts 분리 (타임아웃 + 최대 크기 제한)

---

### 경향 비교 (벤치마크)
- [x] Course 모델에 category 필드 추가
- [x] 유사 교과목 평균 / 학기 전체 평균 / 작년 평균 비교
- [x] 카테고리 내 상위 % 표시 (익명)
- [x] 개선 사례 분석 (학기 간 성과 향상 교수 표시 + AI 인사이트)
- [x] 비교용 시드 데이터 (12교수, 30강의, 646 피드백, 4 개선 사례, 8 교수 노트, 14 라운드, 6 강의자료)

### 교수 노트 (개선 사례 연계)
- [x] ImprovementNote 모델 추가 (courseId, roundId, category, axis, changeDelta, note)
- [x] 주차 리포트에서 이전 라운드 대비 눈에 띄는 변화 감지 (소통 ±0.5점, 이해도/속도 ±15%)
- [x] 변화 감지 시 리포트 카드 하단에 "바꾼 점" 입력 프롬프트 표시 (선택사항, 익명 공유)
- [x] 개선 사례 카드에 같은 분야/다른 분야 교수 노트 카테고리별 구분 표시
- [x] 데모 노트 시드 데이터 (8건: 교양/경영·경제/컴퓨터과학 카테고리, 이해도/소통/속도 축)
- [x] 학기 전체 결산 카드 (주차별 리포트 상단): 전 학기 내 강의 / 전 학기 동일 분야 / 전년 동일 강좌 3단계 비교
  - roundId=null로 ImprovementNote 저장 (학기 레벨 노트 구분)
  - 이전 데이터 없으면 "이전 학기 데이터 없음" 카드 표시

---

### UI 개선
- [x] 대시보드 레이아웃 전체 폭 확장 (max-w-6xl → max-w-[1440px], px-4 → px-8)
- [x] 코스 페이지 상단 KPI 4칸 (총응답 / 소통 만족도 / 이해도 높음 / 속도 적절)
- [x] 코스 페이지 2컬럼 레이아웃 — 왼쪽: 분석 섹션 / 오른쪽: 관리 사이드바(380px)
- [x] FeedbackAnalysis hideTitle prop 추가 (페이지 헤더와 중복 방지)
- [x] Benchmark, ImprovementCases를 왼쪽 컬럼으로 이동 (기존 전체폭 하단에서 변경)

### AI 강화 기능
- [x] AI 한줄평 (AiSummaryLine) — 레이더 차트 위에 표시, [AI] 배지 포함
  - Course.aiSummary DB 캐시: 종료된 라운드 있으면 after()로 백그라운드 사전 계산
  - src/app/actions/radar-summary.ts (computeAndSave, generateRadarSummary, triggerSummaryIfNeeded)
- [x] AI 생성 콘텐츠에 [AI] 배지 표시 (CauseAnalysis, TrendAnalysis, ImprovementCases, AiSummaryLine)
- [x] 모든 AI 시스템 프롬프트에 마크다운 문법 금지 지시 추가
- [x] 규칙 기반 vs AI 비교 기능 삭제 (rule-vs-llm.tsx, rule-vs-llm.ts 제거)
- [x] AI 채팅 (ChatSidePanel / AIChat / useAIChat)
  - 교수가 강의 데이터를 기반으로 AI와 자유 대화
  - SSE 스트리밍, 레이트 리밋 (20회/분/유저), 지표 기반 동적 추천 질문 4개
  - 메시지 복사/내보내기/재시도 기능
  - /api/ai-chat/[courseId] Route Handler
- [x] 강의 대시보드 탭 구조 개편 (analysis-tabs.tsx)
  - 탭1 "피드백 현황": 레이더 차트 + 3축 분석 + 코멘트 + 트렌드 분석
  - 탭2 "심층 분석": 원인 분석, 개선 로드맵 (피드백 3건 이상일 때만)
  - 탭3 "비교 분석": 벤치마크, 개선 사례
  - 수업 체크리스트는 우측 사이드바 RoundReports 안에서 종료된 라운드별로 생성
- [x] AI 개선 로드맵 (ImprovementRoadmapPanel)
  - 피드백 기반 우선순위별 개선 계획 (high/medium/low impact)
  - 행동 항목: area, problem, action, evidence, 주간 목표, 요약
- [x] AI 수업 체크리스트 (class-checklist.ts)
  - 라운드별 AI 생성 체크리스트 (urgent/important/optional 우선순위)
  - 카테고리: content, pace, communication, material + 격려 메시지

### DB 스키마 추가
- [x] Feedback.freeText String? — 학생 자유 서술 원문 (AI 필터링 없음, 대시보드 amber 배경 표시)
- [x] Course.aiSummary String? — AI 한줄평 DB 캐시

### PDF 추출 개선
- [x] pdf-parse → unpdf로 교체 (analyze-material.ts) — Next.js 환경 호환성 버그 해결

### NVIDIA NIM API 연동
- [x] .env AI_PROVIDER=openai + AI_BASE_URL=https://integrate.api.nvidia.com/v1 조합으로 사용
- [x] AI_SETUP_GUIDE.md에 NVIDIA NIM 섹션 추가

### 주차별 트렌드 분석 (AI)
- [x] 종료된 라운드 2개 이상 시 SVG 라인 차트 자동 표시 (이해도/소통/속도적절 3개 시계열)
- [x] "AI 분석" 버튼 → generateTrendNarrative() — 트렌드 방향 판단 + 2~3문장 내러티브
- [x] 라운드 3개 이상 시 다음 주차 예측값 생성 (예측 구간 점선으로 표시)
- [x] 트렌드 배지 (개선/하락/안정/혼재)

---

## 향후 개선 사항

### 기획서 대비 미구현 (우선)
- [x] 원인 연결 분석 (강의자료 + 피드백 교차 분석 → "~일 수 있습니다" 톤으로 가능한 원인 추정)
- [x] 규칙 기반 vs LLM 비교 시연 (동일 텍스트를 두 방식으로 처리한 결과 나란히 보여주기 → AI 필연성 증명)
- [x] 응답률 % 표시 (Course에 studentCount 필드 추가 → "수강생 N명 중 M명 응답 (X%)" 형태)
- [x] 응답률 20% 미만 시 경고 문구 (비율 기반 빨간 경고 + 기존 5건 미만 노란 경고 유지)
- [ ] 참여 유도 장치 (비교과 포인트 등 리뷰 베네핏 — 참여 자체를 유도하는 구조)
- [x] UI 용어 수정 ("진단"→"판단", AI 프롬프트 내 "문제"→"개선이 필요한" 등 톤 통일)

### 코멘트 필터링
- [x] 규칙 기반 욕설/비하 필터 (제출 시 차단 + 감정적 표현 경고 후 확인)
- [x] AI 기반 감정 필터 (학습/감정/혼합 분류 + 혼합 순화)
  - [x] 제출 시점 백그라운드 처리로 변경 (학생 응답 블로킹 없음, DB에 결과 저장)
  - [x] 대시보드에서 AI 호출 제거 → DB 저장된 필터 결과 즉시 표시
  - [x] Feedback 모델에 filteredComment, commentCategory, commentFilterReason, commentHasProfanity 필드 추가
  - [x] 악의적 코멘트 시드 데이터 추가 (욕설/인신공격/비하 등 다양한 유형)
  - [x] "원문 보기" 토글 기능 폐기 → AI 분류기 선에서 처리 (건설적이면 원문 그대로, 욕설 있으면 순화, 비건설적이면 null)
  - [x] comment-classifier.ts 프롬프트 개선: 구어체/비공식체 건설적 피드백은 원문 통과, 욕설/비속어만 필터링
  - [x] 추가 의견 카드: AI 분류 완료된 학습/혼합만 표시, freeText는 teal 배경으로 같은 카드 내 하단에 표시

### 동적 레이더 차트 (다각형 평가)
- [x] Course 모델에 hasAssignment, hasPractice 필드 추가
- [x] Feedback 모델에 interest, assignment, practice 필드 추가
- [x] 동적 레이더 차트 컴포넌트 (radar-chart.tsx, SVG 기반 4~6각형)
- [x] 피드백 폼에 흥미도 항상 표시, 과제/실습 조건부 표시
- [x] 대시보드에 종합 평가 레이더 차트 카드 추가
- [x] 시드 데이터에 강의별 hasAssignment/hasPractice 설정 + interest/assignment/practice 값 추가

### 기타
- [ ] 교수 회원가입 기능
- [ ] 강의 생성/수정/삭제 UI -> 이거는 관리자가 알아서 추가/삭제하는 방식이 더 좋아보이긴 함.
- [x] 학기별 과목 조회 (대시보드 첫 페이지에서 연도/학기 선택 → 해당 학기 과목만 표시)
- [x] 학생 시스템 + 크롬 확장 연동
  - [x] Student, CourseStudent, FeedbackRound, StudentCourseToken, SubmissionLog 모델 추가
  - [x] Feedback에 roundId 추가 (주차별 평가)
  - [x] 다회용 토큰 방식 피드백 제출 (submitStudentFeedback)
  - [x] 피드백 페이지: 활성 라운드 확인 + 중복 제출 차단 + 상태별 화면 분기
  - [x] SubmissionLog로 제출 기록 분리 (Feedback에 studentId 없음 → 익명성 보장)
  - [x] AI 실패 시 commentCategory=null → 교수에게 미노출
  - [x] /api/eclass-sync (크롬 확장 → Student upsert + 수강 등록 + 토큰 발급)
  - [x] /api/student-courses (과목 목록 + 라운드 상태 + 제출 여부 조회)
  - [x] 크롬 확장 프로그램 (사이드 패널, e-class 스크래핑 → 서버 연동)
  - [x] 교수 대시보드: FeedbackRound 관리 UI (startDate/endDate 기반 생성/삭제, 상태 자동 판단)
  - [x] FeedbackRound 시간 기반 자동 종료 (active 플래그 제거 → startDate/endDate)
    - [x] src/lib/round-utils.ts (getRoundStatus/isRoundActive/isRoundClosed)
    - [x] rounds.ts: toggleRound 제거, createRound에 startDate/endDate 인자
    - [x] feedback.ts / 피드백 페이지 / /api/student-courses 시간 기반 쿼리로 교체
    - [x] round-manager.tsx: 열기/닫기 버튼 제거, datetime-local 입력 + 상태 뱃지
  - [x] 시드 데이터: 학생 10명, 수강 등록 20건, 토큰 20개, 라운드 14개 (인공지능 개론 8주차 + 데이터베이스 6주차, 3월 3일 1주차 기준, 8주차만 진행 중)
  - [x] 피드백 제출 확인 다이얼로그 (안내 박스 + window.confirm 재확인, 익명성 보장 위해 수정 기능 없음)
  - [x] 주차별 리포트 (메인 대시보드는 실시간 전체 집계, 종료된 라운드별 요약 카드)
    - [x] src/app/actions/round-reports.ts (getRoundReports)
    - [x] src/app/dashboard/course/[courseId]/round-reports.tsx (RoundManager 다음 위치)
  - [x] 기존 1회용 토큰 시스템과의 공존 — 두 방식 병행 유지 (submitFeedback + submitStudentFeedback)
  - [x] 교수 소유권 검사 추가 (`course/[courseId]/page.tsx`, `materials/page.tsx`)
